import { routeMetadata } from "@/lib/route-metadata";

export const metadata = routeMetadata("/reports");

/** Route stub so the shell is navigable. Content arrives with the Reports module. */
export default function ReportsPage() {
  return null;
}
