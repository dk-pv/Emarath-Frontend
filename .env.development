# Local development defaults (non-secret, safe to commit).
# Automatically loaded by `next dev`. Override locally with `.env.local`.
NEXT_PUBLIC_APP_NAME=Emarath
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api
